slam_toolbox:
  ros__parameters:
    odom_frame:       odom
    map_frame:        map
    scan_topic:       scan
    resolution:       0.05      # map cell size in meters
    max_laser_range:  10.0
    minimum_time_interval: 0.1
    # …more tuning for loop closure, scan matcher, etc…
